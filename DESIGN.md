# Lararium 架构设计

**状态**:v2.0 · 2026-08-17(推翻 v0.1 的多 agent 方案重新设计)
**定位**:开源、自部署、单 agent 多模块的个人生活助手
**读者**:项目作者与未来的贡献者

---

## 1. 定位与设计哲学

Lararium 是跑在你自己服务器上的生活总管:通过一个 IM 对话框管理账单、运动、日程、学习和待办。名字来自罗马人家中供奉家宅守护神(Lares)的神龛——一个容纳诸家神的结构,正如本框架容纳各个领域模块。

对标一句话:**闭源生活助手的开源自部署版**。闭源产品把数据放在别人服务器上且架构不可改;通用自主 agent 什么都能干但边界失控。Lararium 取中间的空档:只做生活管理,但每一块都开放、可换、可审计。

五条硬约束,优先级从高到低,所有设计冲突按此裁决:

1. **单人可维护**:整个系统的心智模型必须装进一个人的脑子。宁可少功能,不加间接层。
2. **十年可运行**:数据格式全部用无聊技术(markdown、SQLite),任何组件死了数据都能徒手迁走。
3. **统一入口**:用户永远只面对一个对话框,系统里只有一个"脑子"。
4. **显式优先**:能用可读文本表达的状态,不用黑盒存储;能全量注入的,不做检索。
5. **缓存命中优先**:上下文按"冻结前缀 + 追加流水"组织,任何设计若破坏前缀稳定性,推倒重来。

**非目标**:不做通用自主 agent——无 shell(唯一代码执行面是受限沙箱,见 §9)、不自主浏览网页;不做多用户 SaaS;不追求接入一切数据源,接不上的用手动导入兜底并坦然接受。

**领域范围**:生活全域(财务、健康、学习、待办日程……),因此**加一个新领域必须便宜**是模块系统的硬指标——扔一个新 bundle 进 compose,主控零改动。

## 2. 总览:两个平面,四种容器

系统流量分两个平面:

- **数据面**:外部原始数据进入各模块。支付通知 → 财务模块,运动记录 → 健康模块。不经过 LLM、不经过主控,纯管道,7×24 静默运行。
- **对话面**:需要"脑子"的一切。用户消息、定时触发、模块事件,统一包成**消息信封**投给主控。主控是对话面唯一的智能体。

两个平面的交点在模块内部:数据面囤原料进模块 SQLite,对话面来查询时现取现算。"记一笔账要过大模型吗?"——不用,数据面;"这笔账正常吗?"——要,对话面。

```
┌─────────────────────────────────────────────────────┐
│                   Gateway 容器                        │
│  渠道适配器(CLI/Telegram/飞书/QQ) + 调度器(cron)        │
│  + ingress(唯一入站端点,逐源 token)                    │
│  职责:生产消息信封,哑转发,零智能                        │
└──────────────────────┬──────────────────────────────┘
                       │ 信封(内网 HTTP → 收件箱)
                       ▼
┌─────────────────────────────────────────────────────┐
│                   Steward 容器                       │
│  收件箱(SQLite,严格串行消费) → 组装上下文 → agent 循环   │
│  独占:起居注 · 内置工具(时间/读skill/搜历史/沙箱代理)     │
└───────┬──────────────────────────┬──────────────────┘
        │ MCP                      │ MCP
        ▼                          ▼
┌───────────────┐   ┌─────────────────────────────────┐
│ Memory bundle  │   │  领域 bundle × N(财务/健康/学习…)  │
│ 账本(markdown) │   │  manifest + skills + 工具         │
│ 快照表·pending  │   │  + 数据面 provider + 独占 SQLite  │
└───────────────┘   └─────────────────────────────────┘
        (另有独立的 sandbox 容器,见 §9)
```

**一轮对话的旅程**:消息到达适配器 → 包成信封 POST 给 Steward → 落收件箱(持久化,**严格串行消费**,任何时刻只有一轮在进行,重放与审计才有清晰顺序)→ 组装上下文 → agent 循环,按需调各 bundle 的 MCP 工具 → 回复经 Gateway 出站发回 → 本轮全部事件追加进起居注。

四种容器职责一句话:**Gateway 只生产信封,Steward 只消费信封,Memory 只守账本,bundle 只管自己的领域。**

## 3. 为什么是单 agent

旧方案(v0.1)是"主控路由 + 每模块内部子 agent + handoff 转接"。推翻的原因:多个脑子之间**传话必丢、交接必漏**——brief 摘漏事实、handoff 中途崩溃丢上下文、转接退出误判困住用户,全部是"传话层"的结构性故障,不是实现问题。

单 agent 后:全系统一个脑子(Steward 的 agent 循环),模块退化为**工具箱 + 数据库**。不需要 brief(账本全量在场)、不需要 handoff(深度会话就是继续聊,领域方法按需读 skill)、不需要逃逸检测(主控每轮在场,听得懂"不聊了")、人格统一是架构自然结果。路由不再是一个环节:主控看到消息直接开工,该调工具调工具——路由就是 agent 循环本身。

子 agent 唯一的真实价值是上下文隔离(在自己上下文里翻几百条原料,只带结论回来)。用更便宜的方式买到:**工具把聚合做在模块内部的 SQL 里,返回结论不返回原料**(§5)。真有领域证明"聪明工具不够用",在该 bundle 背后加子 agent,对外还是同一个工具,契约不变——后手保留,间接层等有证据再加。

## 4. 上下文工程:冻结前缀 + 追加流水

主模型(DeepSeek 系)的前缀缓存按字节匹配,命中部分约一折价格、首 token 延迟大降。追加不毁前缀——这是缓存友好的全部秘密。上下文按"变化频率升序"分区:

```
【前缀区】变更即缓存重建,所以必须罕见
 第0层  工具 schema(API 参数;服务端序列化后占前缀头部)── 启动时冻结
 第1层  system prompt:人格总则 + 插件目录行              ── 启动时冻结
 第2层  核心账本全量                                     ── 批量结算时才变
【流水区】append-only,只增不改,天然命中
 第3层  L1:开放话题状态卡 + 近期话题索引     ── 仅压缩事件时重写(§7)
 第4层  L0:近期逐字对话流(消息·工具调用与结果)── 每轮追加
 第5层  本轮信封(含时间戳)                   ── 新 token,本无缓存
```

**纪律(违反即 bug)**:

- **时间只出现在信封里,绝不出现在前缀区**。信封时间戳随流水冻结,不回改;需要精确时刻或日期推算,调 `current_time` 工具。
- **工具 schema 字节级稳定**:数量、顺序、字段序全部固定。改任何工具定义 = 重启级操作。
- **注册表变更 = 重启**:加/删 bundle、改目录行、改工具,重启全量重建缓存一次,理所应当。SKILL.md **正文**不在前缀里,热更新,随时打磨。
- **账本变更批量结算**:审批通过的提案攒着,随下一次压缩事件或夜间闲时统一落盘,避免零散地毁缓存。
- 计划内缓存重建点全系统只有两个:**账本结算、压缩事件**,且尽量合并为一。

**分层路由**(skill 的装载方式,取代旧方案的 handoff/模式):

```
第一层(前缀,冻结):目录行 —— "财务:记账与消费分析 (skills: 月度复盘/预算规划)"
第二层(按需拉取):read_skill(finance) → 该领域 SKILL.md 全文(领域地图)
第三层(按需拉取):read_skill(finance, monthly-review) → 具体方法正文
```

skill 正文以工具结果身份进入流水,冻结后天然缓存。配一条写进人格总则的纪律:**没在当前上下文里读过的 skill,不许照着干活**。目录行只给路由资格,不给执行资格;压缩冲掉了就重读——重读的边际成本近零(前缀全命中),凭印象干活的错误成本无上限。

**可观测性**:每轮日志打印缓存命中 token 数。缓存命中是硬约束(§1 第 5 条),就必须是硬指标——组装函数改坏了前缀,当天在数字上现形。

## 5. Plugin Bundle 契约

一个 bundle = 一个容器 = 一个 MCP server + 一份自描述。模块对主控的全部存在就是这个捆绑包:

```
finance/
├── manifest.yaml             # 自我介绍,启动时被 Steward 读走后冻结
├── skills/
│   ├── SKILL.md              # 领域总览(分层路由第二层)
│   ├── monthly-review.md     # 单项 skill 正文(第三层,热更新)
│   └── budget-planning.md
├── tools.py                  # MCP 工具:纯函数,零 LLM
├── providers/
│   └── sms_webhook.py        # 数据面:解析通知入库
└── data/finance.sqlite       # 独占存储,其他容器物理摸不到
```

manifest 示例:

```yaml
name: finance
description: 记账与消费分析          # 前缀目录行
skills:
  - {name: monthly-review, desc: 月度消费复盘的方法}
  - {name: budget-planning, desc: 制定和跟踪预算的方法}
tools: [record_expense, query_spending, list_recent]   # 顺序即序列化顺序,冻结
triggers:                            # 调度器替它排班
  - {cron: "0 8 * * *", envelope: daily_brief_data}
events: [unusual_expense, provider_down]   # 它可能主动投递的信封类型
```

**契约要点**:

- **工具铁律:返回结论,不返回原料**。"按类目聚合本月支出"在模块 SQL 里算完,吐十行结果;绝不吐三百条流水让主控扒拉。守的既是上下文预算,也是缓存(流水膨胀加速压缩事件,压缩是缓存重建点)。
- **数据所有权**:每 bundle 独占自己的 SQLite;全系统没有共享可变数据;跨模块流量全部过主控(星型)。派生数据(月度聚合等)只能活在产生它的 bundle 内部。
- **数据面 provider** 住在 bundle 容器里,做成接缝(接口定义 + 可换实现)。逆向/非官方 provider 预设**会坏**:挂了投 `provider_down` 事件信封,数据断档靠备用 provider 或手动补录。
- **事件投递**:bundle 唯一的主动权是往收件箱投信封,投完即止。不许直接触达用户,不许碰别的模块。
- **Memory 也是一个 bundle**:同样的 manifest、同样的工具形状。全系统只有一种组件形态,没有特例。
- 开发期 FastMCP 支持进程内挂载(单进程调试),生产拆容器,同一份代码。

## 6. 记忆系统

### 6.1 全景:一套记忆系统,三种存储产权

**模块库里存的不是记忆,是业务数据**。记忆系统全系统只有一套,零件是账本、pending 队列、起居注:

| 存储 | 产权 | 存什么 | 写路径 | 读路径 |
|---|---|---|---|---|
| 账本(markdown)| Memory bundle | 关于你的一手事实 | 门控(唯一)| 每轮全量注入前缀 |
| pending 队列(SQLite)| Memory bundle | 待审提案(隔离区)| propose | 仅审批呈现 |
| 起居注(SQLite)| Steward | 每轮完整过程 | 每轮自动追加 | search_history / 重放 |
| 各模块库(SQLite)| 各 bundle | 领域业务流水 | provider + 工具 | 工具查询 |

试金石:**"删掉这个模块,这条信息还该存在吗?"** 该存在的是关于你的事实(过敏、家人、偏好),进账本;跟着模块走的是业务流水(某次消费、某次跑步),进模块库。**知识不许下沉到模块,流水不许上浮进账本**——模块撞见用户事实,唯一合法动作是 `propose` 给账本,自己留都不许留。

### 6.2 账本

一个 markdown 文件,轻分节:`身份` / `关系` / `长期偏好` / `正在进行`。分节给人手编时好找、给归档仪式定靶子(十年膨胀集中在"正在进行"节,事情完结就归档出去);模型反正全量通读,分错节零代价。**不建分类系统**——全量注入之下分类不服务检索,做重是负资产。

规则:只存一手事实,派生结论现推不入档("住在 X 小区"入档,"离健身房近"现推);共享实体用 `[[链接]]` 引用;保持小(约 2k token 量级)是全量注入成立的前提,靠"领域的归领域 + 完结的归档"守住。

### 6.3 门控写入

一切账本写入走状态机,无旁路:

```
propose(kind: add|amend|retire, content, provenance)
        ▼
   [pending 区] 隔离:绝不以事实身份进入组装上下文
        ▼
 passed(唯一写账本的代码路径)  |  dropped(起居注留痕后销毁)
```

- **按来源分档**:`用户亲口` → 自动放行,聊天回显"已记下:X",可随口撤销;`不可信数据推出`(短信、邮件、模式发现)→ 硬门控,IM 按钮显式确认。**按钮回调走代码状态流转,不过模型**——注入内容没有机会替用户点头。这是提示注入的最后防线:恶意短信 → 模块 → propose → 账本 → 永久污染每轮上下文,这条链在 pending 隔离区被斩断。
- **审批不阻塞**:平时全异步,硬门控项静静排队(晨报带一句"有 N 条待审")。**压缩事件是唯一同步屏障**:压缩要销毁提案的原始证据(流水),证据销毁前必须结案——自动档结算,硬门控档打包成一张审批卡,审完才继续对话。高水位设在窗口约七成,留缓冲给深夜 cron;顶到硬上限才严格停等。
- **amend/retire 用原文匹配**:提案带"旧文本 → 新文本",落盘时精确匹配;匹配不到说明账本被手编过,提案过期打回重提。不发 ID,不破坏手编体验。

### 6.4 版本管理:快照表(git 退场)

账本正本是 markdown 文件(永远可手编);历史在 Memory bundle 的 SQLite 里一张快照表:

```
ledger_history(时间戳, 全文快照, 来源[审批批次/手编导入/回滚], 提案ID列表)
```

- 回滚 = 取历史行写回文件(本身记为新变更),做成工具,**聊天里说"撤销刚才那条"即可完成**;
- 审计 = 查表;diff = difflib 现算;
- 手编兼容:结算前读文件对内容哈希,与最新快照不符 → 先存"手动编辑"快照再叠加提案。手编不丢、不冲突、进历史;
- 审批流转、快照、结算在**同一 SQLite 一个事务**完成,无两store劈叉;
- 约 30 行胶水,全标准库。备选:dulwich(纯 Python git,需要 git 工具链兼容时可迁,数据都在)。

### 6.5 分拣:双保险与不变量

流水内容的去向:**关于你的认知 → 门控进账本;业务记录 → 调 bundle 工具入库;其余 → 哪也不去,躺起居注等着被搜**。

分拣时机双保险:①对话进行中随手分拣(主力);②压缩前沉淀筛扫一遍将被丢弃的段落(兜底)。由此保证核心不变量:

> 任何有价值的信息,任一时刻**要么还在流水里直接可见,要么已沉进账本或模块库**。压缩必须先完成交接(筛 → 结算 → 压)才允许丢弃。全程无失忆窗口。

### 6.6 起居注与检索

起居注:append-only SQLite,记每轮的信封、工具调用与结果、回复、压缩快照、门控流转。铁律:**可见即入账**——进过模型上下文的一切可从起居注重建;压缩摘要、状态卡按**原文**入账(LLM 重新生成不算重建)。信封 ID 贯穿全链路,即分布式追踪;任一轮可复现、可重放、可 fork——重放日志也是改 prompt/换模型时的回归评测集。

检索:`search_history` 内置工具,FTS5 全文索引 + 主控迭代搜索(搜不到换词再搜、命中后读前后文)。**检索是给日志的,不是给账本的**——账本靠全量在场,召回 100%,不走检索。

**embedding 的前提已翻转(2026-08-18)**:v2.0 原判"等 FTS 被击穿再考虑"成立于账单/待办负载——那些查询天然带关键词。陪伴负载恰是词法检索的最坏情况:对旧话题的引用总是拐弯的、凭印象的(词汇失配),任何词法匹配原理上跨不过去。M3 起 `search_history` 内部改**双路**:词法路(FTS5,赢在精确召回——人名/数字/店名)+ 语义路(sqlite-vec + 本地中文 embedding,赢在模糊召回),并集返回,工具签名不变。embedding 写入时离线算,不进前缀、不碰缓存、不出机器。注意:双路只治"搜了词对不上";"想不起来去搜"由压缩产出的话题索引治(§7)——两个问题,两味药。

## 7. 压缩:话题片段分治

生活对话没有任务主线——一天里记两笔账、批一条事实、聊半小时贝叶斯,是**一袋互不相关的片段**。所以 coding 式滚动叙事摘要(为"继续同一件事"写交接单)不适用;纯丢弃也不行,因为袋里混着极少数还活着的话题。方案:按片段分诊,各找归宿。

- **已闭合片段**(绝大多数):价值在压缩前已被分拣搬空(事实入账、流水入库、逐字稿在起居注)。处置:**丢弃正文,留一行索引**——`8/15 · 月度消费复盘 · 结论:外卖超支,下月预算-500`。索引不是摘要,是搜索的钩子;是列表不是叙事,不需要主线。
- **开放片段**(通常 0~3 个):每个写一张**状态卡**(话题、进展、关键中间结论、下一步)。全系统唯一需要"摘要能力"的地方:单话题、小尺寸、用途明确,难度低一个量级。
- **误判保险**:长期进度本来就不许住在上下文里(学到哪、复习队列是学习 bundle 的业务数据,早该入库)。状态卡只桥接短期延续感,误判为闭合的代价只是"说'继续'时先搜一下旧账",不是失忆。

压缩事件编排(高水位触发,一个事件干完全部):

```
1. 切片段:待压缩区按话题切开(廉价模型,切错无大碍)
2. 沉淀筛:漏网事实→propose,漏网业务→模块工具
3. 审批屏障:pending 清空(§6.3)
4. 分诊:开放→状态卡(正在进行的保留尾部逐字,切点落在片段边界,不腰斩活话题)
        闭合→一行索引,正文丢弃
5. 重写 L1 区,一刀压到低水位,缓存重建一次
```

**开放研究点(方向已定,参数待实验)**:索引保留天数、状态卡尺寸、开放/闭合判据、切片准确率。M3 落地基本版;M4 拿真实日志跑压缩一致性测试(压缩前聊过的事实压缩后答案不变;"继续上次"衔接自然)。

## 8. 主动性

没有任何常驻 agent 循环。主动性 = 调度器往收件箱扔信封:

- **调度器**(Gateway 内,APScheduler):汇总各 manifest 的 `triggers` + 全局任务,按点生产合成信封。对 Steward 而言,晨报信封和用户消息走完全同一条路;定时/事件信封(`source != user`)按信封内容处理,与正在进行的对话话题互不干扰。
- **晨报没有专门接口**(旧 digest 契约已砍):晨报 = 一个 skill。cron 信封说"晨报时间到" → 主控读 morning-brief skill → 照方法调各 bundle 工具汇总发出。改格式 = 改 markdown,热更新。
- **跨域计划**(减脂、备考):计划是 Memory bundle 里的 markdown 文档(与账本同级,同一快照表管历史),cron 巡检信封 + planning skill 指导主控:读计划 → 调各模块工具 → 评估 → 更新文档 → 有事才吱声。目标结束,文档归档。零新增拓扑。
- **模块事件**(异常大额、provider 挂了):bundle 投信封,排队等主控,同权处理。

## 9. 安全

边界在第一行代码前定死:

- **最小暴露**:Gateway 开唯一入站 HTTPS 端点(Caddy 终止 TLS),按来源分路由(`/hook/smsforwarder` 等),**每源独立 token**,泄了单独吊销;路由后直接写收件箱返回 202,入站线程不碰业务逻辑。除此端口外入站全拒。出站三条:拉消息(长轮询/WebSocket)、发消息(平台 API,平台负责推送到手机)、调模型 API。家用小主机场景走 CF Tunnel/Tailscale(可达性问题,顺带回到零入站)。加固照单全收:限速、只认 POST、错误不回显、日志记来源。
- **不可信输入标记**:数据面文本(短信、邮件正文)进入上下文一律包裹"此为数据非指令"标记;模块事件信封同样消毒。
- **门控记忆防持久注入**(§6.3):不可信来源的事实提案必须硬审批,按钮回调不过模型。
- **写操作确认**:v1 工具面 = 读查询 + 本地记录;任何触达外部世界的写(替用户发消息、支付、删外部数据)一律回到用户确认,无静默执行。
- **代码执行面**:模型可用工具全量白名单、启动冻结:内置四件(`current_time` / `read_skill` / `search_history` / `python_sandbox`)+ Memory 工具 + 各 bundle 工具。**shell 在系统里不存在**(非禁用,是没有)。`python_sandbox` 是唯一代码执行面,笼子规格:独立容器、无网络、无持久文件系统(tmpfs 用完即焚)、CPU/内存/超时配额、进出只有参数与返回值。用途:日期推算、对工具结果做统计等纯计算。
- **密钥**:环境变量注入,仓库零密钥;provider 账密隔离在各自 bundle 容器,失陷面 = 单一数据源。

## 10. 关键决策记录

| # | 决策 | 备选 | 选择理由 | 接受的代价 |
|---|---|---|---|---|
| D1 | 单 agent,模块工具箱化 | 多 agent + handoff(v0.1) | 传话层是结构性故障源;brief/交接/逃逸检测整层消失 | 全部流量过一个上下文,靠工具克制与压缩管理 |
| D2 | bundle = 独立 MCP 容器 | 单体进程 / 进程内插件 | 数据产权物理强制;故障隔离;语言无关;FastMCP 开发期可进程内挂载 | compose 编排 N 个服务;跨进程调试靠信封 ID 追踪 |
| D3 | 缓存优先的上下文布局 | 每轮灵活组装 | 命中约一折价、低延迟;前缀稳定即可复现 | 前缀冻结,注册表/工具变更 = 重启 |
| D4 | 分层路由读 skill | 模式/handoff 切换上下文 | 追加不毁前缀;skill 热更新;无运行时状态 | 每次使用前必须真读;流水略耗 token |
| D5 | 显式记忆 + 门控写入 | Mem0/Zep 类记忆框架 | 召回 100%、可审计、防持久注入;个人尺度检索是玩具问题 | 写入依赖 LLM 判断,靠双保险分拣兜底 |
| D6 | 快照表管账本历史 | git / dulwich | 30 行胶水全标准库;与门控同库同事务;聊天内回滚 | 无 git 工具链;dulwich 为备选迁移路径 |
| D7 | 压缩 = 片段分治 | 滚动叙事摘要 / 纯丢弃 | 生活对话无主线;闭合丢成索引、开放写成卡 | 切片与分诊的廉价模型调用;参数待实验 |
| D8 | 最小暴露(单端点+token) | 零入站(全隧道) | 认证入站对个人项目风险可控,免除隧道依赖 | 一个公网端点需保持打补丁;框架认证前漏洞理论暴露 |
| D9 | 主动消息全走 IM 平台 | 自建推送 | 平台即推送基础设施,发=出站 API 调用 | 受平台可用性与限频约束(QQ 单聊主动受限,群方案待测) |
| D10 | 出件箱独立于起居注 | 复用起居注当发送队列 | 投递状态要 UPDATE(pending→delivered→重试),而起居注的不变量是逐字 append-only——混用毁掉重放可信性;回复先落出件箱再标记信封完成,崩溃至多重算一轮,绝不静默吞回复 | 多一张表;同一份回复存两处(职责不同:一份是账,一份是待办) |
| D11 | 单 worker 事件驱动串行 | 请求内同步处理 / 线程池 | HTTP 处理函数只入队立返 202,worker 有活逐条干、没活歇(事件唤醒+超时兜底);严格串行的前提(唯一消费者)得以延续;**结算挪到队列空闲时**——前缀重建发生在没人对话的时刻,最不疼 | 回复必须异步取(长轮询);请求-响应不再一一对应 |
| D12 | 沙箱无网络 = 门控的构成性约束 | 沙箱可访问内网 | 审批走 HTTP 后,`POST /v1/commands` 就是门控的开关;沙箱若有网络,模型可自己 POST 审批端点给自己盖章,门控整个失效。"无网络"从 §9 的防外传条款升级为门控成立的前提,两者绑定 | 沙箱内不能做任何联网计算(本就是非目标) |
| D13 | 记忆中间层先于功能 bundle | 按原 M2(财务先行) | 陪伴使用下,账本(月级)与起居注(逐字)之间没有中间层 = 话题一出 L0 就跌进"靠搜";中间层(话题索引/状态卡)治"想不起来去搜",embedding 治"搜了词对不上",都以压缩为载体 | 财务自动入账推迟一个里程碑;期间记账靠手动 |

## 11. 技术选型

| 组件 | 选型 | 理由 |
|---|---|---|
| agent 循环 | Pydantic AI | 抽象浅、类型安全、模型无关、原生 MCP client |
| bundle 封装 | FastMCP | 十几行成 server;进程内挂载/独立容器同一份代码 |
| 主模型 | DeepSeek V4 Flash(文本)/ MiMo 2.5(需多模态时) | 半旗舰、便宜、自动前缀缓存;OpenAI 兼容接口,可配置换 |
| 廉价任务 | 同档或更低(切片、分诊) | 压缩流水线不值得旗舰 |
| 结构化存储 | SQLite(每 bundle 一文件;起居注 FTS5) | 无运维、易备份、十年可读 |
| 账本/计划 | markdown + 快照表 | 可手编、可审计、可回滚(§6.4) |
| 调度 | APScheduler(Gateway 内) | 进程内,无独立服务 |
| ingress | Caddy(自动 TLS)→ Gateway | 最小暴露(§9) |
| 渠道 | 开发期 CLI;上线候选 Telegram / 飞书 / QQ 群方案 | 适配器可换;微信排除(无官方接口,封号险);QQ 单聊主动受限、群推送 2026-06 开放,待实测 |
| 账单捕获 | SmsForwarder 类通知转发 → ingress webhook | Android 主力机;备用:邮箱账单/CSV 导入 |
| 健康数据 | Android 运动 App 同步方案(实现期选型) | 预设会坏,备用手动补录 |
| 部署 | docker-compose;香港 VPS 主 / 家用小主机 + CF Tunnel 备 | 同一份 compose,只差隧道配置 |

**备份**:全部状态 = SQLite 文件 + markdown,打包 volume 即完整备份,每日 rsync;**每季度一次真恢复演练**(干净机器从备份拉起全栈),写进例行仪式。

## 12. 路线图

每个里程碑收尾时系统处于可日用状态;后续烂尾不损害已交付部分。

- **M1 · 骨架**(已交付):CLI 适配器 + Steward(收件箱/组装/循环/起居注)+ Memory bundle(账本/门控/快照表)+ 内置工具三件(沙箱推迟)。验收:终端对话;"我对芒果过敏"走完门控全流程并在后续生效;任一轮可从起居注逐字重放;**每轮日志打印缓存命中 token 数**。
- **M2 · 前后端分离**:出件箱 + 错误分类与重试 + worker(事件驱动串行)+ 统一 HTTP 协议(消息/取件/命令三端点,逐源 token)+ CLI 降级为普通客户端。验收:双终端本机跑通;进程崩溃后消息不丢不静默;错 token 401;重复投递幂等;缓存命中日志仍每轮可见。
- **M3 · 记忆中间层 + 陪伴**:压缩 v1(话题索引/状态卡,即中间层)+ 人格改写(陪伴语气 + 入档规则补"变化频率"轴)+ 检索双路(FTS5 词法 + sqlite-vec 语义)+ L0 治理。验收:压缩前后记忆一致性;"上次那事"不带关键词也能接上;语义路搜回词法路漏掉的真实案例。
- **M4 · 上线**:VPS 部署 + Caddy + 渠道定型(半天实测 QQ 群限频,不行则飞书/TG)+ IM 适配器 + ingress 加固(限速/错误不回显)+ 调度器与晨报(主动性初版)。验收:手机上全天可用;晨报准时;审批卡真机可用。
- **M5 · 功能 bundle + 深度**:财务 bundle(含数据面 webhook)→ 健康 → 学习 + `python_sandbox` + 计划巡检 + 压缩参数实验(真实日志)。验收:真机通知实时入账;两级查询可用;压缩实验结论回填本文档。

> 2026-08-18 重排(原 M2=财务+上线):陪伴使用把记忆中间层从"省 token 的优化"变成"连续性成立的前提",而功能 bundle 的价值不依赖先后。故:分离(所有前端的地基)→ 记忆(对话框的体验)→ 上线(带着好的体验上手机)→ 功能。详见 D13。

## 13. 开放问题

- 渠道定型:QQ 群方案限频实测(M4);
- 本地中文 embedding 模型选型(bge/m3e 级,数据不出机器)vs API(M3 定);
- 连发合并:先按"一条消息一轮"跑,真机用几天被三连回复烦到了再定合并窗口(M4 观察);
- 压缩参数:索引天数、状态卡尺寸、开放/闭合判据(M5 实验);
- 健康数据 provider 选型(M5);
- 模式发现批处理(起居注周扫描找统计性模式,候选走硬门控)优先级低,随缘;
- 多模态(拍照记账):主模型换 MiMo 2.5 即可尝试,随功能 bundle 可选。

---

*本文档随实现演进,重大偏离以新决策记入 §10。*
