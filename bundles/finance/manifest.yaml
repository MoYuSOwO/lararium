name: finance
description: 记账与消费分析          # 前缀目录行
skills:
  - {name: monthly-review, desc: 怎么看一个月的账}   # M4-4;目录行随之变一次(D3 重建点)
tools: [record_expense, query_spending, list_recent, amend_expense, delete_expense]   # 顺序即冻结顺序(§5 示例)
triggers: []
events: []
