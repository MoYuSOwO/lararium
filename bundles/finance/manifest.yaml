name: finance
description: 记账与消费分析          # 前缀目录行
skills:
  - {name: monthly-review, desc: 怎么看一个月的账}   # M4-4;目录行随之变一次(D3 重建点)
tools: [record_expense, query_spending, list_recent]   # 顺序即冻结顺序(§5 示例)
writes: [record_expense]   # M5-12:这些会真的改数据。留痕用——「说了已记、这一轮却没有写工具跑过」要认得出来
triggers: []
events: []
