name: memory
description: 核心账本与门控写入
skills:
  - {name: writing-facts, desc: 什么该入账本、怎么写才范式化}
tools: [propose_fact, list_pending]   # 只列模型能调的;审批/结算/回滚走代码路径,不入 manifest
writes: [propose_fact]   # M5-12:这些会真的改数据。留痕用——「说了已记、这一轮却没有写工具跑过」要认得出来
reads: [list_pending]   # 只读的那些。和 writes 合起来必须恰好等于 tools:
                                       # ——只挡"填错字"挡不住"加了工具忘了登记",要靠划分
triggers: []
events: []
