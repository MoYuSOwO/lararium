# 开发规范

写代码前读一遍,评审时按条文引用(「违反 S1」)。

## 为什么需要这份东西

Python 没有编译器帮你守边界:任何模块能 import 任何模块,任何函数能写任何文件,
类型注解可以不写、写一半、或者写了骗人。**这些自由在项目小的时候是效率,大了就是腐烂的入口。**

腐烂从来不是一次性发生的,是一百次"就这一次"累积的:
这次图省事加个 `utils.py`,那次循环依赖了就把 import 塞进函数体,
再一次异常懒得处理就 `except: pass`。每一次单看都无所谓,合起来就没救了。

自动门禁(ruff / mypy / import-linter / 架构测试,见 [PLAN.md](PLAN.md))能挡住机械可判定的部分。
**这份文件管的是判断题**——门禁永远挡不住的那些。

一条规则要进这份文件,得同时满足两点:防的是真实的腐烂,且机器判不了。
纯风格问题(缩进、引号、import 排序)不写在这里,ruff 会自动改。

---

## S · 结构

**S1. 禁止 `utils.py` / `helpers.py` / `common.py` / `misc.py` / `tools/` 杂物袋。**

这是 Python 腐烂的头号入口,没有之一。一个函数找不到归属,说明它的职责没想清楚,
而不是说明需要一个杂物袋。放进它服务的那个模块;如果两个模块都要用,
说明它属于一个更基础的、有名字的概念——给它起那个名字(`db.py`、`envelope.py` 就是这么来的)。

杂物袋的可怕之处在于它自我强化:一旦存在,下一个无家可归的函数就理所当然地扔进去,
半年后它有两千行、被所有模块 import、谁也不敢动。

> 本项目已有的 `steward/tools.py` 是例外且仅此一例:它是「内置工具」这个明确概念,
> 不是杂物袋。往里加东西前先问:这真的是一个提供给模型调用的工具吗?

**S2. 单文件超过 300 行要在评审时说明理由。**

不是硬上限,是审查触发器。M1 的每个文件都在 150 行以内。超了先自问:
是不是两个概念挤在一个文件里?通常答案是「是」。

**S3. 内部实现用 `_` 前缀,公开面不用。**

看到没有下划线的函数就知道有人在外面依赖它,改它要查调用方。
`_insert_under_section`、`_render_envelope` 这些是模块自己的事,随便改。

**S4. 不许在函数体里 import 来绕开循环依赖。**

循环依赖是设计问题,不是语法问题。真遇到了,说明有个概念还没被提取出来——
通常是一个 Protocol(见 `steward/ports.py` 的由来)或一个数据类。
把 import 藏进函数体只是把结构问题变成隐蔽的结构问题。

唯一例外:`if TYPE_CHECKING:` 块里的纯类型 import。

---

## F · 函数与数据

**F1. 跨模块传递的数据必须有名字:pydantic model 或 dataclass,不传裸 dict。**

裸 dict 拼错字段名要到运行时才炸,而且 mypy 和 IDE 都救不了你。
`Envelope`、`Turn`、`ModelReply`、`Snapshot`、`Proposal` 都是这个原则的产物。

模块内部临时周转用 dict 可以。序列化边界(存 SQLite、MCP 返回值)用 dict 也可以——
那里 dict 就是目标格式。判据是:**这个 dict 会跨越模块边界被别人读字段吗?**

**F2. 禁止 `**kwargs` 透传链。**

`def f(**kwargs): return g(**kwargs)` 让参数变成不可追踪的——改 `g` 的签名,
没有任何工具能告诉你哪些调用点会挂。要么写明参数,要么传一个有名字的配置对象。

**F3. 超过 3 个参数就用 keyword-only(签名里加 `*`)。**

`Steward.__init__`、`assemble()`、`Gate.propose()` 都是这么写的。
好处是调用点自解释,而且以后加参数不会因为位置错乱而悄悄传错。

**F4. 一个函数要么查询、要么改状态,不要又查又改。**

`Ledger.read()` 只读,`Ledger.write()` 只写。确实需要合并的(`get_or_create` 类),
在名字里说明白。命令查询混在一起的函数没法安全地重复调用,而重放和重试都依赖这个性质。

**F5. 不许有模块级可变全局状态。**

`_cache = {}` 写在模块层,测试之间就会互相污染,而且你永远不知道谁改了它。
需要状态就放进类实例,由组装根(`cli.py`)注入。模块级只允许真常量:全大写、不可变。

**F6. 副作用要写在名字里。**

`settle()` 会写文件,`propose()` 会写库,名字都说了。
一个叫 `check_xxx` 的函数如果偷偷写了库,下一个读代码的人就被骗了。

---

## E · 错误

**E1. 不许裸 `except:`,不许 `except Exception: pass`。**

吞掉异常等于把 bug 埋进未来,而且埋得很深——现象会在别处以离奇的形式冒出来。
要么处理(说明白怎么处理),要么让它冒泡。**只记日志不算处理**,那只是把 bug 埋进日志文件。

`loop.py` 里 `except Exception` 后面跟着「记起居注 + 标记信封失败 + `raise`」——
这是处理:留痕、不卡队列、然后照样往上抛。

**E2. 模型可调的工具反过来:不许抛异常给模型,要返回人话错误。**

这是本项目的特殊约定。模型看到「读取失败:没有这个 bundle: finance;已注册: ['memory']」
能自己纠正重试;看到 traceback 就整轮炸了,用户看到的是助手死掉。

见 `BuiltinTools.read_skill` 和 `propose_fact` 的写法。注意这条只适用于**工具边界**,
工具内部调的函数该抛还是抛。

**E3. 异常消息要带上下文和可能的出路。**

```python
raise KeyError(f"没有这个 bundle: {bundle};已注册: {sorted(self._by_name)}")   # ✓
raise KeyError(bundle)                                                          # ✗
```

你是在给三个月后的自己写,那时候你已经不记得这段代码了。

---

## D · 依赖

**D1. 加一个依赖前先算:自己写要多少行?少于 200 行就自己写。**

十年可运行的约束下,每个依赖都是未来的维护债——它会停止维护、会破坏性升级、
会在 Python 4 出来时把你卡住。账本历史不用 git 而用快照表,就是这条的直接产物:
三十行标准库代码,换掉一个庞大的外部依赖。

**D2. 第三方库只准出现在一个适配模块里。**

Pydantic AI 只出现在 `steward/model.py`,FastMCP 只出现在 `bundles/*/server.py`。
库升级改 API 时,你只需要看一个文件。这条同时也是类型宽松档的边界(见 PLAN.md)——
适配层脏一点是设计意图,但脏必须被关在盒子里。

---

## T · 测试

**T1. 测行为,不测实现。**

断言「输入 X 得到 Y」,不断言「内部调用了 `_foo()`」。测实现的测试在重构时全体变红,
于是人就开始改测试来迁就代码——那一刻测试就从资产变成了负债。

**T2. 不许 mock 自己的代码。**

mock 第三方边界(模型 API)是对的——`FakeModel` 替换的是 Pydantic AI 那一侧。
但如果你需要 mock 自己的模块才能测,说明耦合太紧,该修的是设计不是测试。

**T3. 测试名说明行为,一个测试一个主题。**

`test_claim_is_strictly_serial` ✓ / `test_inbox_2` ✗。
测试失败时,名字应该直接告诉你什么行为坏了,不用点开看代码。

**T4. 修 bug 前先写复现测试。**

顺序是:写一个失败的测试证明 bug 存在 → 修 → 测试变绿。
没有复现测试的修复,你不知道自己修没修对,也挡不住它回来。

**T5. 不许写 `assert True` 或「调用一下不报错就算过」的占位测试。**

这种测试比没有测试更坏:它把覆盖率数字撑起来,让人以为这块有保障。

---

## A · 架构条文(设计的执行细则)

这些是 [DESIGN.md](DESIGN.md) 的可执行版本。改动涉及它们时,评审要专门核对。

**A1. 前缀区是禁区。** 任何改动 `assembler.py`、工具 schema、注册表输出的提交,
必须在提交信息里说明对前缀字节的影响。缓存命中是设计约束第 5 条,不是优化项。

**A2. 账本只有一个写入路径:`Gate.settle()`。** 别的地方要写账本,先问为什么不走门控。

**A3. 知识不下沉,流水不上浮。** 关于用户的事实只进账本,业务记录只进模块库。
模块在对话里撞见用户事实,唯一合法动作是 `propose`,自己留都不许留。

**A4. 工具返回结论,不返回原料。** 聚合在模块的 SQL 里做完再返回。
「把三百条流水丢给模型自己算」既烧上下文又烧缓存。

**A5. 时间只在信封里。** 前缀区出现任何时间戳都是缺陷。

**A6. 模型见过的一切必须能从起居注重建。** 包括未来的压缩摘要、状态卡——
按原文入账,不能指望「到时候再生成一遍」(LLM 重新生成的东西字都不一样)。

**A7. skill 正文是 markdown,不是 Python。** 「怎么做月度复盘」这类方法论写进
`skills/*.md`,因为它会被频繁打磨,放文件里可以热更新且不动缓存前缀。
把方法论写进代码等于把它冻在重启周期里。

---

## L · LLM 相关

**L1. prompt 不硬编码在代码里。** 人格在 `prompts/`,领域方法在 `bundles/*/skills/`。
Python 里只留拼装逻辑。判据:这段文字是给模型读的吗?是就进文件。

**L2. 能算的不要问模型。** 日期推算、金额聚合、统计——用 SQL 或 Python 算。
模型用来做判断和表达,不用来做确定性计算:它更慢、更贵,而且会算错。

**L3. 模型输出是不可信输入。** 模型说要写账本 → 走门控;模型给的 skill 名 → 走白名单
校验(不是字符串过滤);模型给的 id → 查库确认存在。
把模型输出当可信输入,等于把提示注入直接接到你的写入路径上。

**L4. 每次模型调用都要可观测。** token 用量、缓存命中、耗时进日志。
LLM 项目最容易失控的是成本和延迟,而这两样只要不看就一定会烂。

---

## 通用

**G1. 注释写「为什么」,不写「做什么」。**

代码已经说了做什么。注释要说的是:为什么用这个不用那个、这里有什么坑、
这个奇怪的写法在防什么。

```python
# isolation_level=None:自己管事务,claim 要用 BEGIN IMMEDIATE     ✓
# 连接数据库                                                        ✗
```

**G2. 不留注释掉的代码。** git 记得,你不记得。注释掉的代码块会让后来者
(包括三个月后的你)不敢删也不敢用。

**G3. 标识符用英文,注释和文档用中文。** 本项目的既定风格,保持一致。

**G4. 抑制检查必须带理由,范围最小。**

```python
result = legacy_call()  # noqa: S310 - URL 来自本地配置,不是用户输入
```

禁止:整条规则从 `select` 删掉、整文件 `# type: ignore`、
把架构测试的白名单当垃圾桶。这些是在拆门禁,不是在修问题。

**G5. 不做没有需求的抽象。** 「以后可能要支持多个 X」不是理由。
真到了那天再抽象,那时你才知道正确的抽象长什么样。
本项目的子 agent、embedding 检索、多渠道适配都是这么处理的:留接缝,不预付。

---

## 怎么用这份文件

- **写之前**:扫一眼 S 和 F 两节,它们防的是最常见的腐烂。
- **评审时**:按编号引用(「这里违反 F1,`meta` 应该是个 dataclass」)。
  编号让评审意见变得可讨论——被评审的人可以反驳「F1 说的是跨模块,这个不跨」。
- **规则错了就改**:某条规则如果反复挡住合理的做法,那是规则的问题。
  在 [REVIEW.md](REVIEW.md) 里提出,改完记进 [CHANGELOG.md](CHANGELOG.md)。
  **一份没人敢改的规范会先变成摆设,再变成笑话。**
