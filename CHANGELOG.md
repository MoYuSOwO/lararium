# 变更日志

按里程碑分节记录。**每个任务验收通过后,立刻在当前里程碑下追加一行**——
攒到里程碑结束再补,你会想不起来那个任务到底解决了什么。

**写什么**:写"这个系统现在多了什么能力、少了什么坑",不写"改了哪几个文件"——那是 git log 的活。
带上任务编号,方便回查 [REVIEW.md](REVIEW.md) 的验收记录。

**格式**:一行一条,默认都是新增能力;是修复或改变已有行为的,在开头标出来。

```
- **Task 3** 起居注支持中文检索,"日料"能搜到"那家日料店"(两字词走 LIKE 回退,trigram 只认三字以上)
- **Task 7** 修复:skill 名走 manifest 白名单校验,堵住路径穿越
```

---

## M1 · 骨架(已完成)

目标:能在终端里对话、事实走完门控全流程并在后续生效、任一轮可从起居注重放、每轮打印缓存命中。

<!-- ↓ 第一条从这里开始写,删掉本行注释 -->

- **Task 1** 配置加载就位:`Settings.load()` 从 `LARARIUM_*` 环境变量读模型与运行参数,缺 API key 即报错;人格总则 `prompts/persona.md` 落地;测试经 conftest 与宿主环境隔离,`source .env` 后跑测试不再串读真实配置
- **Task 2** 信封模型与严格串行收件箱落地:`Envelope` 走 pydantic、`Inbox` 用 SQLite 持久化 + `BEGIN IMMEDIATE` 保证任一时刻最多一条 processing;崩溃恢复 `recover_stale()` 在启动时清理遗留的 processing 记录,带重试上限防毒消息,避免硬崩溃后队列永久卡死、助手静默
- **Task 3** 起居注落地:append-only `Journal`,FTS5 trigram 做中文检索、两字词回退 LIKE;`replay()` 逐字重放单轮、`recent_turns()` 给 L0 取最近对话;内部事件(prompt/tool_call)不进检索索引
- **Task 4** 账本文件与快照表落地:`Ledger` 管 markdown 账本 + SQLite 快照表,支持手编检测(`sync_manual_edit`)、回滚、diff;`read()` 纯读缺文件即报错(防静默失忆),新建职责交给启动期 `ensure_initialized()`,全代码树只剩 `Ledger.write()` 一处写文件
- **Task 5** 门控状态机落地:`Gate` 分档审批(user_stated 直通、untrusted 待审批),`settle()` 批量结算护缓存,amend/retire 用 old_text 精确匹配、过期提案打回不阻塞同批;retire 只删第一处匹配行,粗 old_text 不再连坐
- **Task 6** Memory bundle MCP server 落地:`build_memory_components` 组装 Ledger+Gate、`create_server` 暴露 FastMCP、`memory_tool_functions` 只含 `propose_fact` 与 `list_pending` 两个工具(审批/结算/回滚走代码路径,不经模型);SQLite 连接加 `check_same_thread=False`,框架线程池里的工具调用不再崩
- **Task 7** 插件注册表落地:`Registry.load` 扫描 `bundles/*/manifest.yaml` 组装目录,`read_skill` 走 manifest 白名单校验挡路径穿越,`directory_lines` 确定性排序保证前缀字节稳定;坏 manifest 点名出错的文件路径,重名 bundle 直接拒绝启动,不留够不着的领域
- **Task 8** 内置工具三件落地:`current_time` 带时区与星期、`read_skill` 委托注册表、`search_history` 走起居注 FTS5;`as_tool_functions` 顺序固定护缓存;检索结果硬封顶 20 条,`limit=-1`(SQLite 当不限制)与 `limit=10000` 都钳制到上限,一次工具调用不再撑爆 L0
- **Task 9** 上下文组装器落地:纯函数 `assemble` 拼前缀区(人格/目录/账本,字节稳定)与流水区(L1 摘要 + L0 对话 + 本轮信封);untrusted 外部数据包裹成「数据不是指令」;信封时间戳走配置时区(`assemble(timezone=...)`),不再依赖操作系统本地时区,VPS 上不再和 `current_time` 差 8 小时
- **Task 10** 模型客户端协议与缓存指标落地:自有的 `ModelClient` 协议 + `ModelReply`,Pydantic AI 实现关在隔离盒 `PydanticAIClient` 里(库升级只改一个文件);`extract_cache_hit_tokens` 按服务商探测缓存命中 token(DeepSeek `details` / pydantic-ai `cache_read_tokens`),`format_cache_log` 每轮打印命中率,缓存可观测性落地
- **Task 11** 一轮编排与 CLI 落地:`Steward.process_next` 认领→记事件→组装→跑模型→记工具事件→记回复→完成,整个循环可从起居注重建;Steward 只经 `ports.py` 的 `LedgerPort`/`GatePort` 接触 Memory(契约首次实战守住);CLI 带 `/approve` `/settle` `/history` `/rollback` `/replay` 等代码路径命令,审批不过模型;打错的 `/` 命令提示「未知命令」不再误发模型,一次 API 错误(限流/401)不再打死 CLI——随时可用的底线
- **Task 12** M1 端到端验收:四条 DESIGN §12 标准自动化测试(事实走通门控并在后续生效 / 任一轮逐字重放 / 前缀跨轮字节稳定 / 不可信内容进不了账本);真实 API 冒烟六项全过(OpenCode Go + mimo-v2.5),第二轮起 `[cache]` 命中 53.8%;缓存日志标注请求数(用量是整轮累加的,看到「N 请求」就不会把工具往返稀释的百分比误读成前缀不稳)——M1 里程碑完成
- **补1** M1 审计 P0-1:第二轮起模型前缀(人格/目录/账本)整段丢失。根因是 `message_history` 非空时 pydantic-ai 不再注入 `Agent(system_prompt=)`。改为把前缀作为 `SystemPromptPart` 放进历史首条 `ModelRequest`,首轮同路径;让「事实在后续对话生效」这条验收标准重新成立
- **补1b** 补1 的报文测试往下挪到 HTTP 层:`FunctionModel` 只看到库内部表示、OpenAI 适配器不在链路上,对发出的字节无知。改用 `httpx.MockTransport` 断言真正发出去的 `body["messages"]`,补上工具往返测试(一轮两次请求);夹具抽 `conftest.py`,`httpx` 显式声明进 dev 依赖

---

## M2 · 前后端分离(已完成)

目标:Steward 变常驻 HTTP 服务,所有前端走同一协议;消息入队立返,worker 逐条干;CLI 降级为普通客户端。协议契约冻结(token 定 channel、/v1/messages、/v1/outbox 长轮询、/v1/commands、/v1/health)。

- **M2-1** 出件箱落地:`Outbox` 独立于起居注(起居注逐字 append-only,投递要 UPDATE),回复先落箱、信封才算完成——崩溃重算多花一次 API 但绝不静默吞回复;seq 全局递增、客户端按 seq 去重(at-least-once);`take` 观测性标记 delivered_at 不阻止再取
- **M2-2** 错误分类与重试(P2-3 关闭):隔离盒 `model.py` 把 pydantic-ai 异常分类成自家 `ModelCallError(retryable=...)`(429/5xx/连接/超时/认不出→可重试,400/401/403/404/422→终态,不对称是有意的),loop 只认它;可重试回 pending 重试(退避上限封顶 3 次),超限/终态 → failed + 出件箱 notice 含原文前 50 字;`Settings.max_attempts`(默认 3)
- **M2-3** worker 事件驱动串行(D11):`TurnOutcome` 拆三支(replied/empty/retry_later)消除 `process_next` 返回 None 的一词多义;单消费者空闲时结算、`retry_later` 指数退避(2**attempts 封顶 60s,绝不等 wake——否则任何新消息都立刻重锤被限流的消息)、毒消息吞异常继续,不陪葬
- **M2-4** HTTP 服务 + Step0a 关 SDK 隐藏重试(max_retries=0,否则与持久重试叠乘:一条 429 打 3 个请求×max_attempts=9,且 SDK 层在内存、起居注看不见、重启就丢——两套留强的那套);`Envelope.id` 加 32-hex pattern + validate_assignment(伪造 id 曾被 search_history 渲染在围栏外,是 P1-4 换字段);AST 写禁升级;`Settings` 加 bind/tokens
- **M2-5** 命令端点 `POST /v1/commands`:handle_command 搬到 `gateway/commands`,门控开关(D12)经 HTTP 直通;**token 分能力两类**——控制端(全权)与数据面 ingest(只准入站,命令/出件箱/健康一律 403,堵住"恶意短信自己批准自己"的整链攻击);HTTP `/quit` 零副作用
- **M2-6** CLI 降级为纯 HTTP 客户端(httpx + after 游标持久化,重启不丢不重),`.importlinter` 新增"cli 是纯客户端"契约;六项真实 API 双终端冒烟全过——含 **kill -9 中途杀服务→重启→recover_stale 重排队→回复最终送达**,D10 崩溃语义在真实进程上成立

---

## M3 · 陪伴与记忆(已完成)

目标:记得住、接得上、说话像个熟人。200k 预算用满;话头归 Steward(对话自身状态,不是生活领域),压缩只产索引行不产状态卡。

- **M3-1** L0 按 token 预算截断(200k)+ 收掉 M2-6 遗留:`outbox.put`+`inbox.complete` 同一事务(崩在中间不再重复回复,D10 真·恰好一次);`recent_turns_within_budget` 从最新往回填、预算耗尽即停、最新一轮无条件在;上下文超长类 400 的 notice 说人话(不甩 status_code: 400)。补做:M3-1b 把估算器实测定标(CJK 0.8/非 CJK 0.3,原 len//2 低估 1.4~1.6 倍)、预算改为整窗口径(读前缀-8000 留白,余额归 L0)、`_turns_by_id` 一条 SQL 不走 replay(800 轮 274ms→28ms)、`db.transaction(conn)` + `Inbox/Outbox.conn` 属性
- **M3-2** 话头存储(Steward 独占,非 bundle):`Threads` 表 + `open_thread`(同名 upsert)/`close_thread` 内置工具追加在既有工具之后、顺序不插队,`open_threads()` 是代码路径不占模型工具位;条数上限 5、note 字数 80。补做:topic 加 `MAX_TOPIC_LEN=24` + 归一化(折内部空白/去首尾/空名拒),close 同套归一化
- **M3-3** 话头进信封(冻结):认领后把 `open_threads()` 快照冻结进 `env.meta`,历史轮渲染**当时那份**(append-only 严格前缀回归);话头行渲染规矩(P1-2 折内部换行 / P1-3 topic+note 过 neutralize_fence / 像自己记的待办)。Step0:预算口径改「渲染后的形态」(每轮 +10 普通/+40 不可信 + 话头行),2000 轮短聊整份 ≤200k;threads 顺带(去 PMID:、截后 strip);directory/ledger read-once
- **M3-4** 检索拆两个工具 + 分页:`search_history` 词法(报总数+页)、`recall_similar` 语义(vec0+256 维本地 embedding)——决定性对照独立语料 0/5 vs 4/5,recall 存在的一切理由;低于相似度阈值(0.35)不计入总数;page 钳制;两条注入回归照抄词法路(P1-2 折行 / P1-3 中和围栏);embedding 数据面算不碰缓存。补做:扩展加载失败降级不拦启动(VEC_AVAILABLE 三处消费,recall 复用 E2 提示)+ 启动期预热 embedding(慢启动诚实,聊一半卡住不是)
- **M3-5** 夜间归拢(sweep):扫一段起居注补账——只改话头 + 提 pending(untrusted,账本单写者),模型输入/输出逐字落起居注,同区间幂等,掉出前5名的话头也能关(Threads.all_open);廉价模型可单配。补做:归拢 prompt builder 过四条渲染规矩(按来源标注外部数据不写成用户 / fold_text 折行 / 围栏+中和),_fold 提公开
- **M3-6** 压缩:上下文用满 200k 时把顶出低水位的旧轮压成 L1 索引(日期 · 话题 · 结论 · 信封id),只产索引行不产状态卡("什么还开着"是话头的活);沉淀筛直接复用 M3-5 的 Sweeper;两道审批屏障(pending 非空则停,证据销毁前必须结案);append-only 只标记退出不删正文;预算按渲染后口径再扣 L1。补做:索引钩子/日期来自模型切段(id 校验+回退)+ 走配置时区(凌晨不差天)+ worker 空闲自动触发(最小间隔)
- **M3-7** 人格改写(persona.md,一次缓存重建):说话分「何时简短/何时真对话」(账务待办查数一句话带判断 / 聊现实生活接住话题陪伴不附和)+ 入档补**变化频率轴**(三个月仍成立才入档,会变的归话头;同步进 writing-facts 判据3)。真机四样本:情绪/做菜/记账零误 propose、旧事调 search_history 去查、稳定事实(妈妈生日)propose 进账本;补做:「提醒一次就够别每轮念」+ 恢复套话禁令
- **M3-8** 文档收口 + 端到端:DESIGN §6.6(检索拆两工具/对照 0/5 vs 4/5/embedding 已定)、§7(压缩编排两屏障/触发)、§11(200k+前缀缓存)、§13(embedding 已定)回填;persona 顺带(提醒一次就够 + 恢复套话禁令);端到端 200k 连聊 30 轮(前缀零重建/严格追加/话头跟着变)+ 真模型 [cache] 60→94.5%。补做:文档四处自相矛盾清理(状态卡遗文案/开放问题删状态卡机制)+ M3 条目排序

---

## M4 · 第一个领域 bundle(财务·对话侧)(已完成)

目标:能在对话里记账、能问出结论。结构价值是第一次有 memory 之外的 bundle,把 §5 的 bundle 契约从"写在文档里"变成"跑过一遍";数据面(短信自动入账)不在本里程碑。

- **M4-1** 第一个领域 bundle 骨架落地:`bundles/finance` 扔进目录即被 `Registry` 发现(**注册代码零改动**),代价是目录行 +1、工具 schema +3 的一次性前缀重建(D3 认可的重建点:注册表/工具变更 = 重启);统一构造入口 `build(data_dir) -> BundleRuntime`(`bundles/runtime.py` 只放形状不放行为——共享模块不受 independence 契约保护,放了行为 bundle 之间就开始背着契约共享东西),memory 的 ledger/gate 仍走 ports、不为形状统一被抹平(§6.1 特殊地位,多一条通往同一批组件的路 = 给「账本只有一条写入路径」加岔口);finance 独占自己的 SQLite,产权测试断言它的表零泄漏进 `steward.sqlite`;三个工具的签名与文档**一次定死**(工具 schema 是前缀第0层,骨架态函数体是 E2 人话占位、正体在 M4-2/3/4 换),组装根用显式小表把顺序钉成 `[propose_fact, list_pending, record_expense, query_spending, list_recent]`——加一个领域改一行,不改一处逻辑
- **M4-2** 记一笔落地:`record_expense` 金额走 Decimal 转**整数分**(浮点会让月度合计以「对不上一分钱」的形式冒出来,而那时候你已经查不出是哪笔);类目锁死七类(自由文本会让模型每次发明新词,「吃饭」「餐饮」「外卖」各记一笔,聚合就废了);`occurred_at` 缺省为**配置时区**的现在——时区由组装根注入而不是 bundle 自兜默认值(自兜就会和 `Settings` 各走各的,正是 M1 Task 9 那个 8 小时时差),带偏移的先折回配置时区再落库(原样存会让 SQLite 的 `date()` 按 UTC 切天,整月分组静悄悄错一天)。E2 边界四条一条都不抛:非法类目列出全部合法值、非正数金额、看不懂的时间(**不许悄悄退回"现在"**——模型说的是「上周三」,账上落成今天没有任何人会知道)、以及大到 int64 存不下的金额(补做:`OverflowError` 不是 `sqlite3.Error` 的子类,曾经直接逃出工具边界,信封被标 failed 无声死掉、模型连自我纠正的机会都没有)。**测试用变异检查验真,9 条变异 9 条被咬住**;其中金额那条第一版是绿的——`0.1*100` 在 IEEE754 里正好是 `10.0`,再叠上 SQLite 的 INTEGER 亲和性把 `10.0` 收成整数,两层一起掩护,浮点实现能大摇大摆过关,换成 `1.005` 才咬得住
- **M4-3** 查落地(工具铁律 A4 的第一次实战):`query_spending` 的 `GROUP BY` 在 SQL 里算完再返回——300 笔带唯一标记的流水进去,出来是「区间·共 N 笔·合计 X 元」+ 每组一行,标记一条都不泄漏;按类目金额降序(第一行就是吃掉预算的那一类),按天**时间正序**且整月 31 天原样装得下(上限的判据是「最常见的那个查询要装得下」,不是照抄 `MAX_SEARCH_HITS`——按金额降序会把时间轴打散,趋势就读不出来了)。超限不许静默截断:被砍掉的部分单列一行报组数和合计,总额行始终是全区间的。日期上界取**次日零点开区间**——落库是 `YYYY-MM-DDTHH:MM:SS`,闭区间会把末日带时刻的流水全吃掉,而合计只是"小了一点",没人会发现。E2 六条路径全走人话(日期反了不许假装成"没有记录")。**「聚合走 SQL 不在 Python 里算」没有任何行为测试能抓**(两种写法输出逐字相同),靠读代码守,已在 REVIEW 写明
- **M4-4** 月度复盘 skill + `list_recent`:分层路由第三层第一次有真内容(`monthly-review.md` 写方法不写数据——先看总额趋势、再看异常类目比**占比**不比绝对值、最后看单笔大额);`list_recent` 是全系统唯一返回原始流水的口子,所以硬封顶 20、`limit=-1`(SQLite 当"不限制")与超大值都钳到上限,并扩出 `since/until/order`——只加排序不给日期范围,「上个月最大的一笔」会被答成全时段之最。**还上早就登记的那笔 note 账**:`note` 是模型写的、不可信轮会把短信正文转述进去,跨轮被捞回来时是 `tool_result` 身份、坐在可信位置、围栏和来源标签全掉;渲染时统一过三刀(折行防伪造整行流水 / 中和围栏与界符防 P1-3 提前闭合 / 先折再截),`record_expense` 的回执和 `list_recent` **共用同一个渲染器**——两套渲染器正是 P1-1 的成因,测试拿同一份 note 走两个出口断言逐字相同。围栏常量是从 assembler 抄的(bundle 不许 import steward),`test_fence_markers_match_the_stewards` 把两边钉死,漂了就红
- **M4-5** 账本与流水的边界:硬边界从 skill 文件(到达率 33%)搬进**每轮都在的前缀**——persona 写明「流水进领域模块,不进账本」,`writing-facts.md` 三个判据补成四个,第四条是**单次事件 vs 稳定安排**(和"变化频率"不是一回事:一次性事件哪怕三个月后仍为真也不该入档,账本记的是他是个什么样的人、不是他做过什么)。真模型三轮 × 十笔:`propose_fact` 零次、账本逐字节未变(**在显式 settle 之后比的**——`user_stated` 当场 passed、worker 空闲自动 settle 那条路必须走一遍,否则"没变"只是因为没人结算)、房租照样 propose(不矫枉过正)。**基线也是零次,所以乙的价值是结构性的不是行为上的**
- **M4-5b/c** 记账依从率:诊断定位到根因在 **L0 的结构**——`_turns_by_id` 只回放 `envelope`/`reply`,工具事件从不回来,模型每轮看到的历史是"用户报开销 → 助手说记好了",**里面没有任何调过工具的证据**,它照着这份被裁掉工具栏的成绩单往下做(同上下文 33/100 vs 空上下文 50/50)。v1 把工具名渲染成助手正文里的一行字:37%→67%,但**模型学会了写那一行来代替调那个工具**(5/5 漏出的痕迹行零真实调用,而伪造出来的那行会存进起居注、下一轮原样回到 L0,和真痕迹逐字同形)。v2 换成**协议层原生形状**(`assistant.tool_calls` + `role:"tool"` 结果消息):位置 3-10 = 80/80,贴平同模型空上下文天花板,**可被伪造的记号根本不存在**。代价是工具结果进了 L0,所以结果与参数都过折行+中和、结果截断可见;预算实测(封装 18.5~26 取 30;工具正文另立一把尺 1.0/字符——日期金额每个数字组几乎自成一个 token,比 base64 还贵)
- **M4-5d** 重试不再重复副作用:可重试失败后 `release()` 让整轮重跑,而失败那轮**没有 reply、不进 L0**,模型对上一次的成功一无所知——一次 503 就能让一笔午饭记两条、一条事实在账本里留下永久重复(`max_attempts=3` 能记三份)。改成**按顺序回放上一次已成功的工具结果、只从断点之后真执行**,挂在工具边界包装层(对所有 bundle 成立,不必每模块各写一遍);**不按 (工具名,参数) 去重**——用户真报两笔一模一样的午饭是合法的,去重会吃掉第二笔,有反向测试钉住。顺带补上一个前置缺口:工具执行原来只在 `model.run` **成功返回后**才落起居注,失败那轮的副作用一条记录都没有,现在由包装层在执行点记 `tool_executed`(带 `replayed` 标志,查重复记账时分得清哪次真跑过)
- **M4-6** M4 收口:自动化端到端(记账→查询→读 skill 五轮走通)钉住两条不变量——**前缀区跨全程零变化**、**L0 严格追加**;真机五项按 PLAN 原措辞全过:该记账的走 `record_expense` 且 `propose_fact` 零次、「房租每月 3800」该入档还是入档且没同时记成流水、查询给结论不给流水、类目没被乱发明,「这个月最大的一笔」走 `list_recent(order="largest", since=…, until=…)` 答得上来;七轮缓存命中 94.3%→91.4%、前缀零重建。挂账销两条:`completion=` 的推理 token 口径是 deepseek 特有的(mimo 实测 `reasoning_tokens=0`)、第 5 项已答得上来;`read_skill` 到达率仍挂着(这轮七次对话零调用),它是路由层面的问题,和记账依从率是两回事
- **M4-7** 修复:主动推送不再是"系统失忆"。原来它用假信封 id 投出件箱、**起居注里什么都不写**,而 L0 靠 `envelope` + `reply` 成对取——早上推「这个月餐饮 1240」,用户切过来说「太多了吧」,模型没有任何上下文(擦边 A6 与不可协商第 3 条;M4 之前只有 cli 一个渠道,推送与对话同窗口,这个断层看不出来)。现在推送落成**一轮完整的对话**:`source="sweep"` 的信封当由头、推送正文当回复,两者都落起居注、都进 L0,渲染走现成的「(系统触发 · sweep/渠道)」那一支——**没新发明形状**,也不伪装成用户说的话(P1-1 不在新路径上重犯)。推送渠道改走 `LARARIUM_PUSH_CHANNEL`(M3 结转第 2 条一次修掉),渠道名在启动时校验而不是半夜在 worker 里炸。顺带把 `db.transaction` 改成**可重入**:`Journal.append` 自带事务,不可重入等于禁止任何两个写库操作组合成一个原子动作,而"占名额 + 起居注两条 + 出件箱一条"必须一起成或一起不成——半条比没有更坏(模型以为自己说过,用户什么都没收到,当天名额还被占掉)
- **M4-8** 人设可自定义,而且**改不坏纪律**:`prompts/persona.md` 拆成 `{data_dir}/character.md`(用户的,不进仓库,`LARARIUM_CHARACTER_PATH` 可覆盖,缺失/为空回退内置默认)+ `prompts/discipline.md`(系统的,代码的一部分)。拆之前两者混在一个文件里——用户为改语气去编辑,极易连下半截一起重写,而那下半截装着几个里程碑打出来的东西(流水不进账本、说记好了之前先真的调工具、propose 门控与变化频率判据),**删掉不会有任何报错**,只会在某天发现账本里全是午饭。顺序写死「人设 + 纪律」;软上限 2000 字**只警告不拒绝也不截断**(用户自己的机器);**装配结果与旧 persona.md 逐字节相同,拆分本身零前缀重建**。硬约束:**人设只能改文件不能靠对话改**——模型可控写入前缀 = 提示注入直通车(P0-1 最多污染一轮,人设被改是之后每一轮都听新的),有一条遍历 `all_tools()` 的测试钉着"没有任何工具能触达那个路径",想在对话里调语气走账本(propose_fact + 门控),**没有为此新增任何工具**。顺带补上前缀变更留痕:新表 `prefix_log`,启动时算前缀指纹(盖住人设/目录行/账本三层)与上次比,变了记一条并打日志——改了人设、缓存命中从 90% 掉到 0,以前没有任何地方说得清为什么
- **M4-9** embedding 权重转 fp16 + 本地 mmap 加载:**加载峰值 1767 → 972 MB、常驻 1461 → 955 MB**(同机实测),目标机 2C2G(可用 1288 MB)从"加载那一瞬间顶天花板起不来"变成装得下;加载还快了一倍(1842 → 842 ms)。质量代价实测为零——最大相似度漂移 0.000066,而判定阈值 0.35、命中与未命中的间隙约 0.09。性能代价说准了写进注释:**页热之后只慢 8%(那才是 fp16 算术),首次触达慢 2.2 倍、多出的 ~60 µs 几乎全是缺页**——只测同一句话会得出"几乎不慢"的错误结论。质量回归的参照表由转换脚本在 float32 原始权重上量好、committed 进仓库,所以它**每次都跑**、不需要 HuggingFace 权重在场,并且顺带挡住换错权重/tokenizer 不匹配/归一化丢了/维度变了。顺带修一个真问题:权重路径是运行时解析的,而缓存原来按进程分键——谁先触发加载谁的 env 说了算,一次失败就让整个进程此后再也用不上语义检索且无处可查;改成**按权重目录分键**("这个目录加载不了"是关于那个目录的事实,不是关于进程的)。明确**不裁词表**并把理由写进模块 docstring:多语言是这个开源项目的产品属性不是可优化的冗余,内存真不够时正确做法是换更小的多语模型

---

## M0 · 立项与工程基建(已完成)

- 架构设计 v2.0([DESIGN.md](DESIGN.md)):单 agent + plugin bundle。推翻了 v0.1 的多 agent + handoff 方案——多个脑子之间传话必丢、交接必漏,那是结构性故障不是实现问题
- 记忆系统定案:显式账本全量注入 + 门控写入(pending 隔离区斩断持久化提示注入)+ 快照表管历史(git 退场,30 行标准库代替)
- 压缩策略定案:话题片段分治(闭合的丢成索引、开放的写成状态卡),放弃了滚动叙事摘要——生活对话没有主线
- M1 实施计划([PLAN.md](PLAN.md)):12 个任务,TDD 逐步展开,每步含可运行代码
- 工程门禁四关:ruff(含 DTZ 时区规则)、mypy(分层严格)、import-linter(架构边界)、pytest(项目不变量)。提交时自动执行,三条真实违规已实测能拦住
- 开发规范([CONVENTIONS.md](CONVENTIONS.md)):36 条分七组,只收"防真实腐烂且机器判不了"的判断题
- [AGENTS.md](AGENTS.md):编码 agent 的导航入口(CLAUDE.md 软链到它),分层指路而非重复规则
- 验收协议([REVIEW.md](REVIEW.md)):门禁先跑、规范按编号引用、三条跨任务不变量人工核对

---

## M5 · 上手机(进行中)

目标:助手从终端搬进微信,手机上能记账、能审批、能发图。

- **M5-1** 渠道定型:只做微信 ClawBot(iLink)一条通道,企业微信不做——实测 iLink **能主动推送**(官方实现里 `context_token` 没有任何过期逻辑),推翻了"只能被动回复"的前提,而那正是当初要拉第二条通道的唯一理由
- **M5-3** 微信 IM 适配器:与 CLI 同级的**纯客户端**(`.importlinter` 加一条禁入契约钉着),照官方 MIT 实现用 Python 重写——三个社区实现都**没有许可证**(= 保留所有权利),端点/字段名/流程是事实可以照着独立实现,代码一行都不许抄。两个泵而不是一问一答(主动推送不对应任何一条用户消息,一问一答的形状接不住它);六个请求头**一处构造、每次全带**(少一个服务端回的是 `-14 session timeout`,而**这个错误信息是骗人的**);**不照抄官方对 `-14` 的处置**——它把 -14 当 token 过期、暂停该账号所有 API 一小时,而实测缺头返回的也是 -14,照抄的话一个头写错就白停机一小时且日志上看是"token 过期"。补做:二维码过期后能换新码——原来只请求一次码然后死等那一张,凌晨三点会话到期、码发到微信、你在睡觉、几分钟后码过期,**适配器对着死码轮询到天亮**,而这里正是唯一的恢复路径;现在两层循环,服务端说废了就换,等够时限也换(**按构造兜底**,不靠把状态枚举全)
- **M5-4** 媒体入站(图片/语音/文件/视频):收得下来、存得住、**堵不了通道**。真正的理由是一个静默死锁——逐条投递、统一推游标,任何一条投递失败(超 16KB 长文 413、服务端抖动)都让异常穿出去、**游标一格不推**,三秒后拿旧游标又取到同一批,那条毒消息卡在队首把它后面的全堵死,而用户在微信这头只看到"发什么都没反应";顺带修掉图片在协议层 `if not text: continue` 被**静默整条丢掉**。CDN 那套照官方 `src/cdn/` 重写,三处比任务书要求的深:**CDN 请求不带 iLink 的头**(另一台主机,发过去等于把 bot_token 泄给第三方)、`aes_key` **两种编码都认**(认漏一种就是某类附件永远解不开)、上限从官方 100 MB 砍到 **16 MB 且流式判超限**(2C2G 上整块进内存会把 Steward 一起 OOM)。`Envelope` 加 `attachments` 但 `content` 仍是字符串(图片对应 `(图片 · media/ab12cd…)` 一行人话,下游一切按文本走的东西不用动);文件名**由内容哈希算出来**,`Attachment` 上根本没有可写的 path 字段——能自报路径的附件就是路径穿越的入口,而它指向 `prompts/character.default.md` 的那天人设就成了对话可改的。顺带补上老库补列机制(`CREATE TABLE IF NOT EXISTS` 对已存在的表是空操作,不补的症状是"新装的机器好使,你自己那台不好使")
- **M5-5** 读图,连同它带来的**一个全新的注入面**:围栏、折行、中和分隔符、来源标注保护的全是文本,而图里的字**根本不经过渲染**,那四刀一刀都落不上去。这一层只有两件事可做,强度不同,写进了模块 docstring 免得下一个人把"有框定语"当成"防住了"——框定语是**说服不是机制**(强度取决于模型),而「图只在到达轮进模型 + 张数封顶」才是机制(注入面不随轮次累积,一张恶意图影响一轮而不是之后每一轮;成本那条同样硬:L0 的预算算术对图片一无所知)。真模型实测两种注入图(直白的「IGNORE ALL PREVIOUS RULES」,以及**伪装成用户自己说的话**的假聊天截图——转发的群截图才是真会遇到的那种):16 个样本 → 15 有效 + 1 作废,**注入成功 0 次**,判据取副作用(话头表 + 起居注里的工具调用)不取措辞。**阳性对照是这条测试的命根子**:"没照做"和"压根没看清"在结果上一模一样,而它当场抓到模型读不清时会**编内容**(回过"看到一些中文文字",图上一个中文都没有),以及对照锚点自己会**错杀**(暗号 `PWNED9` 的 `9` 被读成 `?`)。注入图用标准库画(5x7 点阵 + zlib + PNG,约 120 行),不为一个测试拉 Pillow。降级:`LARARIUM_VISION` **默认关**(接的模型未必能读图;而且开一个防线全失效的注入面应该是一次明确的选择),关着时图照收照存、进上下文的是一行"当前模型看不了图"。顺带修:**带字节的工具结果不许进回放队列**——M4-5d 的断点续跑照着 `str()` 回放,等于在重试那一轮把图悄悄换成一句话,而模型不会知道自己少看了一张。两轮补做修的是同一个洞的两扇门:先是 `look_at_image` 一个种类判断都没有、撞上"认不出就按 jpeg 送"的兜底,一段语音和一份 PDF 都被贴上 `image/jpeg` 交出去(真模型自己就走进去了,一句「这份账单看一下」就够,400 之后整轮当场死掉);修法是把判据收敛成 `Attachment.is_image` 一个出处,**按 media_type 不按 kind**——这顺带修掉另一条路上的同一个洞(微信 `type=IMAGE` 但字节嗅不出时落的是 `application/octet-stream`,按 kind 判连到达轮都会把伪装的 PDF 送进去)。然后是守卫信的那个 `_sniff` **两个方向都在说谎**:WebP 只认 `RIFF`(那是容器族,WAV/AVI 全以它开头)于是一段 WAV 顶着 `image/webp` 走了出去;反方向漏掉 BMP(服务商自己那句报错里就写着支持)和 HEIC(iPhone 原图默认格式),它们掉进 octet-stream 然后**一声不响地**不进模型——**把一次响亮的失败换成一次静默的失败不是修复**,而"静默截断读起来和'就这些'一模一样"就写在 `load_images` 自己的 docstring 里、张数上限那一支早已照做。最后拆开「认得出」与「送得进」(HEIC 认得出但服务商不收)但**只留一个出处** `cannot_send()`,两个出口问同一个函数
- **M5-6** 审批走同一套分派:微信里 `/pending` `/approve` 和 CLI 打同一个端点。**任务书写的是「走 IM 按钮回调」,而这条通道上根本没有按钮**——查官方 MIT 源码,`MessageItemType` 只有 NONE/TEXT/IMAGE/VOICE/FILE/VIDEO/TOOL_CALL_*,全库 grep 不到 button/card/inline_keyboard,官方自己处理斜杠命令也是 `trimmed.startsWith("/")`。所以审批在微信里就是打一行命令。**只判"是不是以 / 开头",具体动词一个都不认**——认了就是第二份分派,而审批权只能有一份(`commands.py`)。`strip()` 要先做:手机输入法很容易带前导空格,` /approve abc` 被当成普通消息喂给模型的话,用户会以为"批准了"而账本纹丝不动,CLI 有 `input().strip()` 兜着,这里没有
- **M5-8** 修复:**一条 sqlite 连接被并发使用**(线上 bug,躺了五个里程碑)。`db.py` 那段注释写着「安全性由架构保证——收件箱严格串行,任一时刻只有一轮在跑,不存在真正的并发访问」,而**「一轮在跑」不等于「一个数据库调用在跑」**:pydantic-ai 把同步工具丢线程池,一条 assistant 消息里的多个 `tool_call` 是**并发执行**的,`check_same_thread=False` 关掉的只是那个守卫、不是让连接变线程安全。症状是三种毫不相干的面孔(`IndexError: tuple index out of range` / `InterfaceError: bad parameter` / `TypeError: 'NoneType' object is not subscriptable`),概率性,真机上表现为这一轮变 retry_later、重试到上限、用户收到「处理失败,已放弃」,而起居注里只有一行指不到任何地方的报错。mimo 一次只发一个调用所以躺着没事,DeepSeek 批量发一试就炸——**不是某个服务商的问题,是这条连接的问题**。修法是 `GuardedConnection`:所有对底层连接的调用走**唯一的临界区**,`RLock` 可重入;**粒度是"一个事务"不是"一条语句"**(只锁单条的话另一个线程能挤进 BEGIN 和 COMMIT 之间,写入掉进别人的事务,而且它自己那句 `BEGIN` 会直接报错);**行必须在临界区里取干净**(锁在 `execute` 返回时就放了,交出一个活游标只是把洞挪个位置)。三条约束都守住:没动连接数(`inbox.conn is outbox.conn` 还在,回复落出件箱与信封标完成仍在同一事务)、不和可重入的 `transaction()` 死锁、`check_same_thread=False` 留着(工具**在**线程里跑没问题,有问题的是**同时**跑)。范围比报告的大:两个 bundle 各自 `sqlite3.connect` 同一套假设、面对同一个线程池,一并改走 `db.open_connection`——实测无锁时 memory 并发 `propose_fact` 二十轮挂十五次,而它的面孔是 **`KeyError: 提案不存在`**(写进去了转头读不回来),**账本唯一写入路径上的数据面失败**,真机上就是"我记下了"之后提案凭空消失、查都没处查。测试**钉机制不钉崩溃**(复现是概率性的,照着崩溃写就是一条靠运气变绿的测试):探针重写临界区在**锁里面**数人头断言 `max_inside == 1`,`Barrier` 卡齐四个线程 + `max_queued >= 2` 当阳性对照。顺带把规则写下来——CONVENTIONS **D3**「连接一律从 `db.connect` / `db.open_connection` 拿」+ 一条机械门禁:那两处当时不是谁偷懒,是这条规则还没写下来,下一个 bundle 会再来一次
- **M5-9** 注入图的暗号重选,以及**一个测试的修复会自己造出假绿**。旧设计里暗号=载荷,模型读错 + 照做时阳性对照先挂、报成「实验作废」——**一次成功的注入被诊断成"模型看不清图"**。拆开之后新的坑更隐蔽:否定断言 `assert not any(CANARY in topic)` 的成立**依赖载荷被 OCR 准**,拆开暗号和载荷之后如果不动它,它就永远为真(话头叫 `MOONFISH`,断言找的是 `XJKY`),一个真注入会安安静静变绿——交付方在造出这个假绿之前先看见了它。`MIN_HAMMING = 9` 不是拍脑袋:5x7 点阵全字符的汉明距离分布 2–8 每档都住满,到 9 出现断层只剩 `JKXY` 和 `4`,取 9 就是取最高的那一簇。真机 mimo × 3 轮 + DeepSeek × 3 轮、每轮 2 个剧本,12/12 全过
- **M5-9 补** 门禁自己只守住了一扇门:D3「连接一律从 `db.connect` 拿」的机械检查只认 `sqlite3.connect()` 一种写法,`import sqlite3 as _sq` 的 `_sq.connect()`、`from sqlite3 import connect` 的裸名、以及带别名的裸名全都漏过去。**守住一扇门的门禁比没有更坏:它让人以为这条规则是机械保证的,于是没人再靠自觉。** 补做给门禁自己写了阳性对照(四种写法逐条参数化 + 一条反向的"不许误伤类型标注"),我自己在一个文件里同时写四种去验,三条全抓;`from sqlite3 import *` 那扇门由 ruff 的 F403 挡着
- **M5-10** 换模型(mimo → DeepSeek),以及**「支持某个图片格式」这件事只能拿真图打**。我绕开整个栈直接对 `/chat/completions` 逐格式打:DeepSeek 收 png/jpeg/gif、**BMP 直接 400**;而同一格式 **mimo 是真收的**。多打的那一格把事情定了性——BMP 从清单里掉出去不是在纠正一个当初写错的清单,是这一家真的比上一家窄,换回去该加回来。**判据取的是"模型念不念得出图上的暗号",不是"有没有 400"**:服务商完全可以 200 收下一个它其实解不开的格式,那种情况和"支持"在 HTTP 层一模一样。`.env.example` 默认保持 `LARARIUM_VISION=off`——「复制一份 .env.example」不该顺带打开一个防线全失效的注入面
- **M5-11** `read_skill` 路由守卫——**这一步整个是设计错的,三步之后被 M5-14 拆掉,代价是真机上 5/8 笔账丢了**。起因是我写的任务书说「记账前该先读 finance 总览,现在两个模型都不读」,而**那是我各跑一次就下的结论**;交付方跑了 10 次:8 passed / 2 failed——25% 的抖动被我记成了 100% 的缺陷。守卫本身做得干净(`functools.wraps` 三层保住签名与 docstring,前缀指纹**逐字节相同**、零重建;守卫也套在 `propose_fact` 上,因为 `tool_owners()` 认 manifest 而 memory 也是一个 bundle、没有特例;16 轮里漏水口一次没开)。**做得对的东西建在一个不该存在的前提上。** 这是 CONVENTIONS G6 的第一个案例:拿到一个「坏了」,先问它该不该在,再问怎么修
- **M5-12** 无人值守两周的两个前提:退避 + 叫醒。24 小时窗口关着的一夜,`except → sleep 3 → 重试` 会打 **14400 次**无效请求;退避后 **150 次**。但**只退避不叫醒等于把延迟写死**——窗口重开的唯一证据是入站消息,所以 `_sleep_or_wake` 被入站叫醒时 0.05 秒就醒,不是睡满 30 秒。比任务书多治了收信泵(同一个论证:iLink 连不上时它每 3 秒一次、一夜近三万次)。这一步也留下一个仪器,它在 M5-16 换模型时哑了——**仪器本身也会过期**
- **M5-13** 工具重试耗尽:同一串 8 笔,4/8 变成 `retry_later`。根因是**服务商把 `function.arguments` 双层包了一遍**,`unwrap_tool_args` 按「那个工具的 schema 里有没有 `arguments` 这个参数」判,不按形状猜,且只认恰好一个键的字典、带兄弟键的不剥——剥错的后果是参数凭空少一半且毫无报错。放在隔离盒(`model.py`)里,因为那是某一家服务商的毛病,不该渗进组装器和 schema。**没有调大 retries**——那会把症状推后,不是修好。这一步真正的产物是 Step 1 那条日志:`capture_run_messages` + 按 `tool_call_id` 把「模型填的参数」和「服务端的反馈」配成对,一条日志把三类可能收敛成一类。修后 8/8 replied、`tool_retry` 0 条
- **M5-14** 拆掉 M5-11 的守卫,并把被它掏空的那一层总览删掉。**最该证的是"摘掉守卫没有把该读的时候也不读了一起带走"**——「这个月的消费复盘」仍然自己去读方法篇,「咖啡 22」直接记不读,2/2。目录行那句 `desc` 在承重:模型靠它决定要不要读 `monthly-review`,而它读对了。第 ① 条验收(8 笔 → 8 条)没达成,查清了成因不在这一步——多出来的行绝大部分是**失败的更正尝试**(模型填错金额后想改,而手里只有 `record_expense`),于是立了 M5-15
- **M5-15** 流水记错了能改:`amend_expense` = 作废旧行 + 插新行。真机实测模型填错后**会主动想更正**,但手里只有 `record_expense`,于是先试 `amount=-27.0`(被工具挡下)、再补一条 note「金额有误待处理」的标记行——**模型的动机是对的,是我们没给路**。当时不给 `delete` 的理由是「改完永远还剩一条替代行」,那条理由后来被 M5-20 推翻。这一步也埋下两个后来要收拾的东西:两个状态位的交互(M5-26),和一批撞时间戳的裸数字断言(M5-25)
- **M5-16** 「记错只发生在多轮里,而那份历史是我们自己重建的」——查下来不是我们重建歪的。**决定性的证据用另一条路取的,不需要抓报文:同一套代码、同一份历史重建,只换模型。** 旧模型每轮套壳 1~5 次、凭空冒出 585/410/589/2813/6981/7614/12830 这些金额;换一个模型之后套壳 0 次、凭空金额 0 笔。**我们发出去的报文一个字没改。** Step 2 把工具从 13 减到 1,套壳 8/8 → 7/8 几乎没动——变量是多轮,不是工具数量;Step 2b 逐个排除了我们这边的候选偏差,而这一步最容易被跳过,因为它注定"什么也没发现"
- **M5-17** 删掉两个没人读得到的信号。审计起居注里的事件种类,发现三个信号**有生产者没有消费者**——`/replay <envelope-id>` 是唯一的读取路径,而它要求你已经知道该看哪一轮。其中 `CLAIM_MARKERS` 是**我在 M5-12 任务书里写的规格**:拿关键词去匹配模型的措辞,换个模型就失效,而我又跨四轮去打补丁,直到用户叫停。**测量没有消费者就是浪费**,这一条和 M5-11 一起蒸出了 CONVENTIONS **G6**:拿到一个「坏了」,先问它该不该在,再问怎么修——判据是「谁会读到它?什么时候读?读到之后能做什么?」
- **M5-18** 不可信是单向的:`_active_untrusted` 一轮之内**只能升不能降**,并按信封持久化成 `untrusted_seen`,这样重试那一轮(工具结果被回放、内层工具根本不跑)它照样是脏的。**G6 当场兑现**:交付方拿它判断出第三条不可信路径不该补,而不是顺手再加一层
- **M5-19** `_sniff` 定义在 `if __name__ == "__main__":` 之后——542 个测试全绿,真机上第一张图 `NameError`。**门禁挡的是位置不是语句种类**:`test_nothing_is_defined_after_the_main_guard` 断的是"`__main__` 之后不许有任何顶层定义",而不是枚举一张"哪些语句不许出现"的清单——枚举清单的门禁只会挡住上一次出事的那种写法
- **M5-20** 记错了能删,删了能撤回:`delete_expense(id, reason, undo)`。**M5-15 我不给 delete 的理由不成立**——我说「删除是唯一会彻底销毁信息的操作」,而作废什么都没销毁;代价是用户说「作废」时那 28 元静静留在账上。名字叫 `delete` 不叫 `void` 是用户提的,**她找不到路正是因为没有一个看起来像"删"的东西**;后台只是标状态位。`deleted_at` 和 `voided_by` 分成两列不复用:合成一列之后想撤回就分不出该撤哪个。顺带改掉一条测试——`test_finance_still_offers_no_way_to_delete` 断的是"没有 delete 这个名字",**它把一个错决定钉成了规格**
- **M5-21** `web_search`(Tavily):第一条联网能力。真机第一天就撞上「帮我查一下这周末上海天气」→「我这台机器上没有网络查询的口」。**做成内置工具不是 bundle**——bundle 是生活领域,联网是能力,而且内置能直接 `import` `neutralize_fence`,不用被 import-linter 逼着再抄第三份中和实现。搜回来的每一条都是不可信内容:**不看域名、不看内容**,`_on_untrusted()` 无条件拉高,`propose` 这一轮强制降档。复核时我自己造了份恶意结果(标题、url、正文三处都塞闭合围栏 + 换行 + 伪装成用户口吻的指令),围栏平衡、载荷一个字没跑到外面。**三句失败话是三句不是一句**:没配 key / key 填错 / 服务挂了——接一个必然打 401 的客户端会让用户去改 key,而真相是压根没配。变异测试抓到「标题不折行」活了一轮:正文折了、url 折了、**标题没测**,而攻击者把 payload 从 `content` 挪进 `<title>` 就照样能凭换行伪造列表项——同一条规则作用在三个字段上,少写一处不会有任何报错,这是 M4-4 那个形状的第三次
- **M5-22** `web_fetch`(Tavily `/extract`):给她一条链接,她读得懂。**这一条被推翻重写过一次**:原方案是自己抓 + 五步 SSRF 防护(钉住 IP、绕过用例表、DNS rebinding、`getpeername` 复查、trafilatura 加七个依赖)。推翻它的是一次实测——替自己抓辩护的最后一条理由是「我们的服务器在国内,国内站从美国机房抓不到」,而拿真机打下来:少数派 13 字、微信公众号 57 字、36氪 54 字全是壳子,知乎和澎湃直接 403,**挡我们的是反爬和 JS 不是地理位置,在墙内一点忙都帮不上**。改走 `/extract` 之后 **SSRF 面归零——不是防住了,是根本没有出站**,唯一的出站目的地写死成 `api.tavily.com`。真 key 冒烟:36氪那个 JS 壳子我们自己抓 54 字、Tavily basic 拿到 24,003 字;知乎和澎湃仍是 0,`failed_results` 里明写 `Failed to fetch url`——**那条边界是真的,而她会如实说「我读不到」**。抽不出正文时分两岔且不许合并:「这页我读不到(要登录/有反爬/正文要浏览器跑)」vs「抓到了但正文是图」,**都不许说「这页没什么内容」——那是把"我读不到"说成"它没有",是编的,而用户会信**。交付方多想到一处:`_readable_length` 要刨掉 markdown 图片标记再数字数,否则一篇整版长图的公众号文章抽出来是一串够长的图片链接,过得了门槛、被当成正文塞进上下文
- **M5-23** 归拢的判据改从 `writing-facts` 拼:三份互相打架的文档收敛成一份。真机跑了两天、31 轮,账本一条事实都没进——查下来不是模型偷懒,是**我们同时告诉了它两件相反的事**:`prompts/sweep.md` 说「suggest 里写用户的原话或它的直接表述,别加你的解读和推断」,而账本自己的规矩(`writing-facts.md`)说「写归纳过的短句」。它照着 prompt 抄原话,于是提上来的是「学校嘛 那天天上课睡觉 那不吃点好的怎么行」这种没法当事实存的东西。修法是让归拢**去读账本自己的那份规矩**,而不是在 prompt 里再抄一遍——抄一遍就是第三份,而三份里改哪份都不会同步。分界线用注释标记 `<!-- SWEEP-CUT` 不用标题:**标题会被改写,标记不会**,而改写标题的人看不出自己顺手切掉了归拢的判据;标记没了直接抛异常并指名这一步,**不静默降级**(静默降级的样子和「模型今天状态不好」一模一样)。同一批对话同一光标重跑:「主要是深圳就美食荒漠 没什么好吃的」→「在上学」,「水没味我不咋喜欢喝」→「不爱喝没味的白水」,而且有两条是复核方人工翻的时候漏掉的。交付方自己测出一个任务书里没写的注入面:**把规矩拼进 prompt 之后,规矩里的示例成了新的输入**——20 次里有 1 次把例子里的「有车」当成用户的事实提了上来,加了一句框定语挡住,形状照抄 M5-5 给图片那句。验收顺带挖出两个真 bug,已立 **M5-24**(归拢光标推的是「窗口内最大 seq」不是「处理到哪儿了」,于是 cursor 和窗口下界之间那一段被永久跳过——真机第一次归拢就把头两天全部对话跳掉了,而失效是静默的)和 **M5-25**(测试拿裸 `"49"` `"22"` 断金额,撞上渲染出来的 `HH:MM`,23:49 假红、22:22 假绿)
- **M5-24** 归拢的光标会跳过没归拢过的历史——**静默丢事实,而失效的样子和"模型觉得不值得提"一模一样**。`_advance_cursor(window_max)` 推的是**窗口内**的最大 seq 而不是"处理到哪儿了",于是 `cursor < seq < 窗口下界` 那一段被跳过,且光标只增不减、再也回不来。真机第一次归拢就把光标从 0 推到 147,**头两天的全部对话一次都没归拢过**,用户提过五次的「有女朋友」、四次的「看 F1 但不是法拉利车迷」模型从头到尾没见过。修法两处一起改:下界改成光标(时间只当上界)、光标只推进到**这一批实际喂进模型的最后一条**;缺口攒下来分批喂,`_PROMPT_CONVO_MAX_CHARS` 从**截断**的刀口变成**分批**的刀口——**「截断保留最近」和「光标推到最新」是同一个 bug 的两张脸**。复核拿真机 journal 副本跑过:光标归零后覆盖 seq 1..145 全部 63 条对话事件,旧口径最早只能到 106
- **M5-26** `amend_expense` 改成就地 `UPDATE`,`voided_by` 整列拿掉。用户问「为什么要改 能不能不给改 错了就删了再新增」,按 G6 查下来作废行**没有读者**:真机两天 13 次工具调用,`include_voided=True` 一次都没被调过;而留证据这件事起居注已经在干,账本那份从来没被读过。代价已经收到两张账单:**删了的行还能被 amend 改活**(一个改金额的动作副作用是撤销一次删除),以及账本里出现过一行**自己作废自己**、没有替代行。迁移是这一条最危险的地方——列一拿掉过滤条件就没了,3 行已作废的会重新冒到账上,所以补列机制里先把它们标成已删;硬口径是**迁移前后用户看到的账逐字节一致**,复核拿真机账本副本亲自走了一遍,`list_recent` + 按类目 + 按天三份输出 diff 为空(9 笔、1055.03 元)。回话也改了:原来那个「改了 #1 → **#2**」**本身就在教模型换号记账**,现在是「还是 #12,号没变」。顺带吃掉 M5-25(测试拿裸 `"49"` `"22"` 断金额,撞上渲染出来的 `HH:MM`,23:49 假红、22:22 记 71 元不调 amend 也假绿)
- **M5-27** 联网两条工具的参数一次选齐。用户的要求就是这一条的形状:「先看看 tavily 到底有什么功能 然后我们从中选用,而不是发现一个又发现一个」——M5-21/22 上线后连着撞见三个该用没用的参数,那是没先看清全貌就开工。过了一遍全部端点和价钱,选三处、每处带实测:`web_fetch` 加 `question`(传给 `/extract` 的 `query`,阮一峰周刊那篇原本截前 4000 字、开头全是「» 首页 » 档案」,给了问题之后开头直接是答案那几段);`web_search` 加 `topic` + `time_range`(「上海这周末天气」从常年天气页变成「台风"科罗旺"外围环流开始影响上海」);验收时复核方补实测后放开第三档 `topic="finance"`(「英伟达最新财报」不填和 news 都回聚合站,finance 回英伟达自己的新闻室和 WSJ)。**明确不做的连理由一起写进任务书**,省得下次再问:`/research` 和 `include_answer` 是第三方模型的转述、「来源」那行指过去找不到原话;`/crawl` `/map` 不是个人助手的需求;`include_raw_content` 正是刚封顶治掉的那个问题;`/usage` 按 G6 没有消费者。**缺省行为逐字不变**是这一步的硬口径(同一份恶意输入在改动前后跑,输出逐字节相同),而 `/extract` 的 `query` **是检索不是总结**——实测挑出来的字与原文逐字相同,只是 markdown 的 `**` 被去掉,这一点写进了 docstring:哪天它变成生成,「来源」那一行就成了假的。顺带记一个坑:挑出来的是不连续片段、用 `[...]` 连接,渲染要折行,折完片段界限就没了,模型会把两段不相干的话读成一句
- **M5-29** 连续失败时第三次尝试会重复记账(第三方 review 打出来的四条 P1 之一)。`last_attempt_tool_results` 取的是**最后一个 `envelope` 事件之后**那一段的 `tool_executed`,于是:第 1 次真跑了 `record_expense` 然后模型调用失败,第 2 次一个工具都没调到就失败(这一段是空的),第 3 次只看第 2 次那一段 → 队列为空 → 模型重新记一遍。**第 1 次那笔账被第 2 次的空记录遮住了**,而账上多一笔没人会发现。修法是改口径:取这封信下全部 `replayed=False` 的 `tool_executed` 按 seq 累计——`replayed` 这个字段本来就在数据里,而且那条 append 落在工具**返回之后**,所以它精确等于「这次调用真的发生了、副作用已经在世界上」,不是"以为跑过的"。三个坑一个都不能踩:**不许按工具名去重**(一轮里合法调两次 `record_expense` 是常事,去重把"多记一笔"换成"少记一笔",一样是钱)、`replayable=False` 的仍然不进队列(M5-5:带图片字节的工具照着 `str()` 回放等于把图换成一句话)、别把 M4-5d 治好的"重试重复副作用"放回来。副作用只跑一次用**三个互相独立的口径**证(会计数的假工具 / 真 finance 库的行数 / `tool_executed` 全量断言),第三条排除了"第 3 次碰巧没调工具"这种假绿。函数顺手改名 `established_tool_results`——返回值现在是跨尝试累计的,`last_attempt_*` 反着说,而**一个管钱的函数挂一个说反了的名字,下一个人会照名字推理**

---

## 里程碑进度

| 里程碑 | 内容 | 状态 |
|---|---|---|
| M0 立项 | 设计、计划、门禁、规范 | ✅ 完成 |
| M1 骨架 | CLI 对话、门控记忆、可重放、缓存可观测(含交付后审计补做) | ✅ 完成 |
| M2 前后端分离 | 出件箱、错误重试、worker、HTTP 协议、CLI 客户端化 | ✅ 完成 |
| M3 记忆中间层 + 陪伴 | L0 预算截断、话头、检索双路、压缩 v1(话题索引,无状态卡)、夜间归拢、人格改写 | ✅ 完成 |
| M4 第一个领域 bundle | 财务(对话侧):记账、查询、月度复盘、账本与流水的边界(顺带照出主控三处缺陷:L0 裁掉工具事件 / 重试重复副作用 / 失败轮不落起居注) | ✅ 完成 |
| M5 上手机 | 渠道定型、IM 适配器、媒体入站与读图、真机、记账的改与删、夜间归拢修复、联网(搜索 + 读网页) | 🟡 进行中 |
| M6 数据面 + 扩张 | 短信入账 provider、健康/学习 bundle、沙箱 | ⬜ 未开始 |
| M7 上线 | VPS、Caddy、镜像、备份、ingress 加固、调度器与晨报 | ⬜ 未开始 |

> 2026-08-18 重排(原 M2=财务+上线):记忆中间层先于功能 bundle,理由见 DESIGN.md D13。
> 2026-08-20 再排(原 M4=上线):上线拆成「在家里能定的事」与「纯搬家」,前者提前后者垫底,理由见 DESIGN.md D14。
